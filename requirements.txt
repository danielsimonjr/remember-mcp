# Core dependencies
numpy>=1.24.0
sentence-transformers>=2.2.2
fastmcp>=2.10.5
python-dotenv>=1.0.0

# OpenMemory - Published on PyPI
openmemory-python>=1.0.0

# Memvid - Use fork with numpy 2.x support
# Official PyPI: memvid>=0.2.0 (not compatible with numpy 2.x)
# Fork with numpy 2.x support:
git+https://github.com/danielsimonjr/memvid.git@main

# Additional
schedule>=1.2.0
